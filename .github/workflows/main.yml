name: Simple CI Workflow

on:
  push:
    branches:
      - main

jobs:
  mytestbuild:
    runs-on: ubuntu-latest

    steps:
      - name: Print a test message
        run: echo "Hello from your GitHub Actions workflow initiated by $GITHUB_ACTOR!"
